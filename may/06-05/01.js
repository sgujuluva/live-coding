function Drilling(anotherFUnction) {

}

function customMap(someFunction, array) {

    for (let i = 0; i < array.length; i++) {

        someFunction(array[i])
    }

}

// array.map is returning something in the inner loop
// array.reduce
// array.filter

// Reduce use cases
// total of a cart
// the average 
// the max/min
